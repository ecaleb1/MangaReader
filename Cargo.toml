[package]
name = "MangaReader"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[dependencies]
iced = {version = "0.13.1", features = ["debug", "image", "svg"]}
zip = "2.2"
directories = "5.0"
