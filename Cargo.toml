[package]
name = "MangaReader"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[dependencies]
iced = {version = "0.12.1", features = ["debug", "image", "svg"]}
zip = "2.1"
directories = "5.0"
